#include <iostream>
#include <vector>

using namespace std;

// 0 也是可以的

class Solution {
public:
    int pivotIndex(vector<int>& nums) {
        int left_sum = 0;
        int right_sun = 0;
        // nums.

        return -1;
    }
};
int main(int, char**){
    vector<int> nums = {1,2,3};
    auto out = Solution().pivotIndex(nums);
    cout << out << endl;
}
